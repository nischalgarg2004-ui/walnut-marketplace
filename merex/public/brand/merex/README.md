# Merex brand assets

The app currently reuses abstract round logos from `../ongram/` via `src/lib/brand.ts`.

When Merex-specific artwork is ready, add files here and update `LOGO_ROUND_*` in `brand.ts`.
