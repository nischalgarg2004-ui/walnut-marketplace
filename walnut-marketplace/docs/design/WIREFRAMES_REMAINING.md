# Remaining routes — wire notes (Part C3)

| Route | Mobile note |
|-------|-------------|
| Applicants + drawer | Replace drawer with **full-screen sheet** or **sub-route** on small viewports |
| Deliverables split | Stack brief → asset → actions vertically |
| Campaign wizard | Single column; sticky step indicator at top |

All routes use **BusinessAppShell** or **CreatorAppShell** for consistent sidebar + header.
